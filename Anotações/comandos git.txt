git add . > adiciona uma pasta nova ou varios arquivos novos de uma so vez / atualiza um arquivo

git commit -m "comentario no commit" / usar para atualização 

git status 

git add "codigo.nn"

git remote add origin https://github.com..

git push > envia os commits pra nuvem

git init > inicia um arquivo .git

git config --global user.email "seuemail@email.com"

git config --global user.name "username"

git push > update

git reflog > ver historico

pegar o id visto no reflog
git reset --hard + id > volta a versao desejada